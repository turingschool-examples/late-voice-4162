<h1>Items</h1>
<% @items.each do |item| %>
<div id="item-<%=item.id%>">
  <h3><%= item.name %></h3>
  <h4><%= item.price %></h4>
  <h4><%= item.supermarket.name %></h4>
  <h4>Purchased by <%= item.customer_count %> customer(s)</h4>
</div>
<% end %>