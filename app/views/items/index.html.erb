<h1>Items</h1>

<% @items.each do |item| %>
  <div id="item-<%=item.id%>">
    <h3><%= item.name %></h3>
    <p>price: $<%= item.price %> </p>
    <p>supermarket: <%= item.supermarket.name %></p>
    <p>item's customers: <%= item.customer_count %></p>
  </div>
<% end %>